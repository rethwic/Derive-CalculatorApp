# Copy to .env.local for local development with `vercel dev`.
# In production, set this in the Vercel project's Environment Variables
# settings instead — never commit the real key.
#
# Get a free key at https://aistudio.google.com/apikey (no credit card
# required for the free tier).
GEMINI_API_KEY=
